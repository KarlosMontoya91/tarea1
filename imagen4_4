function setup() {
  createCanvas(400, 400);
  background(250);
  stroke(255,0,0);
  strokeWeight(32);
  ellipse(200,200,300,300);
  fill(0);
  line(100,100,300,300);
  strokeWeight(30);
}

function draw() {
  //background(220);
}
